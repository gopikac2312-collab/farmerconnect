# Remove the bad git setup
rd /s /q .git

# Start fresh
git init
git add .
git commit -m "FarmerConnect frontend - full project"

# Connect to your existing GitHub repo
git remote add origin https://github.com/gopikac2312-collab/farmerconnect.git

git branch -M main
git push -u origin main --force
```

